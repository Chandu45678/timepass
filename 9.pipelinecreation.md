pipeline {
    agent any
    tools {
        jdk 'JAVA_HOME'
        maven 'MAVEN_HOME'
    }
    stages {
        stage('git repo & clean') {
            steps {
                bat "rmdir /s /q MavenJava || exit 0"
                bat "git clone https://github.com/ANIRUDH-7600/MavenJava.git"
                bat "mvn clean -f MavenJava/pom.xml"
            }
        }
        stage('install') {
            steps {
                bat "mvn install -f MavenJava/pom.xml"
            }
        }
        stage('test') {
            steps {
                bat "mvn test -f MavenJava/pom.xml"
            }
        }
        stage('package') {
            steps {
                bat "mvn package -f MavenJava/pom.xml"
            }
        }
    }
}

WORKING ON BUILD TRIGGERS FOR LAST JENKINS PIPILINE
H * * * *
------------------------------------------------------------------------------------------------------------------------- 
pipeline { 
agent any 
tools{ 
maven 'MAVEN-HOME' 
} 
stages { 
stage('git repo & clean') { 
steps { 
//bat "rmdir  /s /q mavenjava" 
bat "git clone provide your github link" 
bat "mvn clean -f mavenjava" 
} 
} 
stage('install') { 
steps { 
bat "mvn install -f mavenjava" #project name# 
} 
} 
stage('test') { 
steps { 
bat "mvn test -f mavenjava" 
} 
} 
stage('package') { 
139 
 
140 
 
            steps { 
                bat "mvn package -f mavenjava" 
            } 
        } 
    } 
}